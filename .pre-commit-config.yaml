---
# Pinned to specific commit SHA to avoid mutable reference warnings
repos:
  - repo: https://github.com/racktopia/.github
    # Pin to current HEAD to avoid warnings, allow up to 10 commits behind
    rev: 176c6601908da7ab5c1006fc797c7f9f8360feda
    hooks:
      - id: racktopia-standards
      - id: markdownlint
      - id: yamllint
      - id: detect-secrets
