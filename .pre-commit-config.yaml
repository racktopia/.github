---
# Pinned to specific commit SHA to avoid pre-commit warnings about mutable references
repos:
  - repo: https://github.com/racktopia/.github
    # Pin to current HEAD to avoid warnings, allow up to 10 commits behind
    rev: af583fbe14b184df425b968ac609cd55458b803a
    hooks:
      - id: racktopia-standards
      - id: markdownlint
      - id: yamllint
      - id: detect-secrets
